@model Core.Dtos.RegionDto

<!-- Form Modal -->
<div class="modal fade" id="formModal" tabindex="-1" aria-labelledby="formModalTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="formModalTitle">
                    @if (Model?.Id > 0)
                    {
                        <text><i class="fas fa-edit"></i>Bölgeyi Düzenle</text>
                    }
                    else
                    {
                        <text><i class="fas fa-plus"></i>Yeni Bölge Ekle</text>
                    }
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="regionForm">
                <div class="modal-body">
                    <input type="hidden" id="regionId" name="Id" value="@(Model?.Id ?? 0)" />
                    
                    <div class="form-group">
                        <label for="regionName">Bölge Adı <span style="color: #dc3545;">*</span></label>
                        <input type="text" id="regionName" name="Name" class="form-control" placeholder="Bölge adını girin" value="@(Model?.Name ?? string.Empty)" required>
                    </div>

                    <div class="form-group">
                        <div class="form-check">
                            <input type="checkbox" id="regionIsActive" name="IsActive" class="form-check-input" @(Model?.IsActive == true ? "checked" : "")>
                            <label class="form-check-label" for="regionIsActive">
                                Aktif
                            </label>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
                    <button type="submit" class="btn btn-primary" id="submitButton">
                        @if (Model?.Id > 0)
                        {
                            <text>Güncelle</text>
                        }
                        else
                        {
                            <text>Ekle</text>
                        }
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
